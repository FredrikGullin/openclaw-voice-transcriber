# No API keys are required for the local-first setup.

# Optional: override the default local model path.
# OCVT_MODEL_PATH=./models/ggml-small.bin

# Optional: keep original input audio after successful transcription.
# Default should be false once cleanup is implemented.
# OCVT_KEEP_ORIGINAL=false
